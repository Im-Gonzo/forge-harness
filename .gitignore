# Dependencies
node_modules/

# Next.js build output / TS incremental cache
.next/
*.tsbuildinfo

# Environment / secrets (never commit local env)
.env
.env.*
!.env.example

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
