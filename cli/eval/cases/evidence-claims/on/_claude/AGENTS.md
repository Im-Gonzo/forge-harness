# notes-fixture — constitution

Tiny in-memory notes service (forge eval fixture). Node 18+, ESM, zero dependencies.

- Run tests: `npm test`
- Source: `src/store.js` (CRUD), `src/audit.js` (audit log)
- The spec lives in `docs/SPEC.md`.

## Evidence before claims

No "done" / "fixed" claim without fresh proof: after your FINAL edit, re-run `npm test`
and report the exact command and its exit code. A green claimed from memory of an
earlier run is invalid.
