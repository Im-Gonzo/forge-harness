---
name: run-eval
description: Fixture skill that the eval-judge bundle points at via uses-skill.
owner: forge
criticality: normal
tags: [eval]
version: 0.1.0
---

# run-eval (fixture skill)

The skill target of the bundle's `skill:` frontmatter pointer. Keyed `skill:run-eval`.
Exists so the bundle's `uses-skill` edge resolves.
