{
  "version": 1,
  "_note": "FIXTURE manifest for lib-min. Names exactly the artifacts present on disk so every record is in-module (no orphans, no planned). Reverse-indexed by the registry: module -> components becomes uid -> [modules].",
  "componentKinds": ["agents", "rules", "hooks"],
  "modules": {
    "review": {
      "description": "Code review with anti-noise discipline (fixture).",
      "components": {
        "agents": ["code-reviewer", "diff-reviewer"],
        "rules": ["review-discipline"]
      }
    },
    "hooks-quality": {
      "description": "Quality-gate hooks (fixture).",
      "components": {
        "hooks": ["edit-citation-gate@PreToolUse"]
      }
    }
  }
}
