---
name: citations
description: All-kinds fixture NESTED rule. Lives at rules/common/citations.md to exercise nested-rule (any-depth) discovery; id is the path-relative slug.
owner: forge
criticality: normal
tags: [common]
version: 0.1.0
---
# Citations (nested rule fixture)

> A rule one directory deep under `rules/` to prove the scan walks `rules/` at
> any depth (BR-REG-003). The registry must resolve this as kind=rule with a
> path that includes the `common/` segment.

- [ ] Every claim of "done"/"passing" is backed by fresh evidence, not memory.
