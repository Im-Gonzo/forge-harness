{
  "name": "dangling-ref-fixture",
  "version": "0.1.0",
  "description": "FIXTURE plugin manifest. Version aligned with VERSION and package.json so the only validate-registry advisory is the PLANTED dangling ghost-reviewer ref (retargeted EVAL-DEP-001)."
}
